# Server-side only — never exposed to the client.
ANTHROPIC_API_KEY=sk-ant-...

# Phase 2: live adapters (leave unset to run on the demo dataset)
FIGMA_TOKEN=
FIGMA_TEAM_ID=
ASANA_TOKEN=
ASANA_WORKSPACE_GID=
